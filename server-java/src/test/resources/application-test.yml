spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        connection:
          isolation: 2  # READ_COMMITTED
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.testcontainers: OFF
