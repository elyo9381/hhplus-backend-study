spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  test:
    database:
      replace: none
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.testcontainers: OFF
